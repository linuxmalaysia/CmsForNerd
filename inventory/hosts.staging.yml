all:
  children:
    staging:
      hosts:
        cms-stage-01:
          ansible_host: 192.168.10.50
          ansible_user: lab-admin
      vars:
        env: staging
        cms_domain: staging.cmsfornerd.local
