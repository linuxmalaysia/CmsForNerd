<div id="content">
    <h1>About CMSForNerd</h1>
    <p>This is a small, database-free PHP CMS used as a learning laboratory. This page is an example of the "pair logic" pattern: `about.php` + `contents/about-body.inc`.</p>
    <p>Use `$ctx` (CmsContext) inside templates for dynamic values; sanitize any user-supplied values via `SecurityUtils`.</p>
</div>
